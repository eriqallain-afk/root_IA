schema_version: '1.0'
outputs:
- name: PRIMARY_DELIVERABLE
  format: YAML_STRICT
  required: true
- name: LOGS
  format: YAML_STRICT
  required: true
notes: Skeleton auto-généré pour satisfaire l’intégrité des références. À enrichir
  (inputs/outputs détaillés).
