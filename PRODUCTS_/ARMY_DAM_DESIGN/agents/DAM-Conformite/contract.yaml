# Contract I/O — DAM-Conformite
# Vérifie la conformité RBQ, code du bâtiment, règlements municipaux

input:
  objective: string
context: string

output:
  Structured response per instructions below

output_format: yaml
