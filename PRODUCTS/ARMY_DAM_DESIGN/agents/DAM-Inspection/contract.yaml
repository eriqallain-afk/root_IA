# Contract I/O — DAM-Inspection
# Crée le plan d'inspection et les checklists par étape

input:
  objective: string
context: string

output:
  Structured response per instructions below

output_format: yaml
