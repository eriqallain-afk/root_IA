# Contract I/O — DAM-Planification
# Calendrier projet : dépendances, jalons, chemin critique

input:
  objective: string
context: string

output:
  Structured response per instructions below

output_format: yaml
