schema_version: '1.0'
generated_at: '2026-03-22T00:00:00Z'
playbooks:
  INTAKE_ROUTE_EXECUTE:
    description: 'Flux standard : route -> exécute -> archive'
    steps:
    - step: route
      actor_id: OPS-RouterIA
    - step: execute
      actor_id: OPS-PlaybookRunner
    - step: archive
      actor_id: OPS-DossierIA
  IT_CHANGE_EXEC:
    description: Planifier/exécuter un change -> validations -> reporting -> archive
    steps:
    - step: assess
      actor_id: IT-Commandare-TECH
    - step: dispatch
      actor_id: IT-NOCDispatcher
    - step: implement
      actor_id: IT-Commandare-TECH
    - step: report
      actor_id: IT-ReportMaster
    - step: archive
      actor_id: OPS-DossierIA
  IT_COMMANDARE_NOC:
    description: 'Commandare NOC: triage/diagnostic initial, sévérité, plan de réponse.'
    steps:
    - step: execute
      actor_id: IT-Commandare-NOC
  IT_COMMANDARE_OPR:
    description: 'Commandare OPR: gouvernance ops, communication, coordination & contrôle.'
    steps:
    - step: execute
      actor_id: IT-Commandare-OPR
  IT_COMMANDARE_TECH:
    description: 'Commandare TECH: troubleshooting/RCA, plan de remediation, risques.'
    steps:
    - step: cadrage
      actor_id: IT-Commandare-TECH
    - step: execute
      actor_id: IT-Commandare-TECH
    - step: archive
      actor_id: OPS-DossierIA
  IT_CW_INTERVENTION_LIVE_CLOSE:
    description: 'ConnectWise : LIVE journal + closeout CW + KB'
    steps:
    - step: intervention
      actor_id: IT-MaintenanceMaster
    - step: closeout
      actor_id: IT-TicketScribe
    - step: kb
      actor_id: IT-KnowledgeKeeper
  IT_INCIDENT_TRIAGE:
    description: NOC triage -> dispatch -> remediation -> reporting -> archive
    steps:
    - step: triage
      actor_id: IT-Commandare-NOC
    - step: dispatch
      actor_id: IT-NOCDispatcher
    - step: execute
      actor_id: IT-Commandare-TECH
    - step: report
      actor_id: IT-ReportMaster
    - step: archive
      actor_id: OPS-DossierIA
  IT_MSP_LIVE_ASSIST:
    description: "Assistance MSP temps réel (appel/chat/remote) : triage guidé, notes ticket, comms, escalade, KB draft."
    steps:
      - step: execute
        actor_id: IT-MaintenanceMaster
  IT_MSP_TICKET_TO_KB:
    description: Ticket MSP -> diagnostic -> communication -> knowledge
    steps:
    - step: support
      actor_id: IT-AssistanTI_N3
    - step: closeout
      actor_id: IT-TicketScribe
    - step: kb
      actor_id: IT-KnowledgeKeeper
  IT_NOC_DISPATCH:
    description: 'NOC dispatch: prioriser/assigner/escalader et suivre SLA.'
    steps:
    - step: triage
      actor_id: IT-Commandare-NOC
    - step: dispatch
      actor_id: IT-NOCDispatcher
    - step: archive
      actor_id: OPS-DossierIA
  IT_NOC_HANDOFF:
    description: Handoff NOC (fin de quart/escalade) -> synthèse -> reporting -> archive
    steps:
    - step: summarize
      actor_id: IT-Commandare-NOC
    - step: dispatch
      actor_id: IT-NOCDispatcher
    - step: report
      actor_id: IT-ReportMaster
    - step: archive
      actor_id: OPS-DossierIA
  IT_MAINT_PATCH_REBOOT_VALIDATE:
    description: "Maintenance Windows (patching via CW RMM) : plan -> precheck -> patch -> reboot (si requis) -> postcheck -> closeout."
    steps:
      - step: plan_et_live
        actor_id: IT-MaintenanceMaster
      - step: closeout
        actor_id: IT-TicketScribe
      - step: archive
        actor_id: OPS-DossierIA
  IT_POST_SHUTDOWN_VALIDATE:
    description: 'Post-shutdown électrique : baseline réseau/stockage/virtualisation -> services critiques -> monitoring -> rapport.'
    steps:
      - step: triage
        actor_id: IT-Commandare-NOC
      - step: validate
        actor_id: IT-MaintenanceMaster
      - step: report
        actor_id: IT-ReportMaster
      - step: archive
        actor_id: OPS-DossierIA

  IT_N2_SUPPORT_TELEPHONIQUE:
    description: "Support N1/N2 téléphonique : guide étape-par-étape → escalade si P2→P1"
    steps:
      - step: execute
        actor_id: IT-AssistanTI_N2
      - step: escalade_si_requis
        actor_id: IT-AssistanTI_N3
      - step: closeout
        actor_id: IT-TicketScribe

  IT_SECURITY_INCIDENT_RESPONSE:
    description: "Réponse incident sécurité : triage → containment → investigation → remédiation → postmortem"
    steps:
      - step: triage
        actor_id: IT-Commandare-NOC
      - step: response
        actor_id: IT-SecurityMaster
      - step: infrastructure
        actor_id: IT-Commandare-Infra
      - step: report
        actor_id: IT-ReportMaster
      - step: archive
        actor_id: OPS-DossierIA
